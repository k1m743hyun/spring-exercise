sourceSets {
    main {
        resources {
            srcDir('src/main/java')
        }
    }
}

bootJar {
    archiveFileName = 'application.jar'
}

clean {
    delete files('out', 'src/main/generated')
}
