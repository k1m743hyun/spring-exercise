sourceSets {
    main {
        resources {
            srcDir('src/main/java')
        }
    }
}

bootJar {
    archiveFileName = 'batch.jar'
}

clean {
    delete files('out', 'src/main/generated')
}
